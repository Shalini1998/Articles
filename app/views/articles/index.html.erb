<%- model_class = Article -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>

<!-- <table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th>Author</th>
      <th><%= model_class.human_attribute_name(:image) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= link_to article.id, article_path(article) %></td>
        <td><%= article.title %></td>
        <td><%= article.description.html_safe %></td>
        <td><%= article.author %></td>
        <td><%= article.image %></td>
        <td><%= article.created_at.strftime("%b-%d-%Y") %></td>
        <td>
          <%= link_to t('.show', :default => t("helpers.links.show")),
                      article_path(article), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_article_path(article), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      article_path(article),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<% @articles.each do |article| %>
  <div class="article-container">
    <%= image_tag article.image %>
  </div>
  <div class="article-details">
    <h5>Posted By <%= article.author %></h5>
    <h5>Posted On <%= article.created_at.strftime("%b-%d-%Y") %></h5>
    <div class="article-container">
    <h1><%= article.title %></h1>
    </div>

  </div>
  <div>
     <div class="article-container">
    <h5><%= truncate(article.description, length: 500).html_safe %></h5>
    <div style="margin-top: 30px";>
    <%= link_to 'Continue Reading', article, class: "btn btn-success btn-lg" %>
    <div style="margin-top: 30px";>
  </div>



<% end %>

<% @lastArticle.each do |article| %>
<h1><%= article.title %></h1>
<% end %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_article_path,
            :class => 'btn btn-primary' %>
